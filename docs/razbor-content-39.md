# Разбор мастер-плана CONTENT-39

02.09.2026. В мастере 118 строк в 5 типах — ТГ-посты 27, ТГ-видео 21,
статьи 25, YouTube 23, шортсы 22; у строки: приоритет [P1/P2/P3], заголовок,
посыл, показ, козырь.

## Статусы — не путать

- **CONTENT-11 — согласовано**: 25 тем постов, `[x] Аппрув пула от @vladimir`.
  Единственный согласованный источник тем. Тексты мешка пишутся по нему.
- **CONTENT-39 — черновик-пример**: так написано в шапке, ни одна строка не
  согласована. Из мастера берём две вещи: **формат ведения** (тема → выдачи
  «куда → когда → номер задачи», приоритеты, типы материалов) и **черновой
  пул кандидатов** на будущие согласования.

## Зачем тогда сопоставление

Чтобы видеть, что уже покрыто согласованным пулом, а что в мастере — новое
и ждёт согласования. Итог сверки всех строк:

- **23 из 25 согласованных тем C11 имеют строку-черновик в мастере** —
  формулировки мастера НЕ заменяют согласованные каркасы C11, это просто
  черновые варианты тех же тем. Все 12 тем ротации на месте; 8 из 12 стоят
  у шефа в [P1]/[P2] — приоритеты бьются.
- **Наших тем 23 (QNote) и 24 (рабочий стол) в мастере нет** — они
  согласованы в C11, из работы не выпадают; в мастер допишутся строками.
- **Нового в мастере, чего нет в C11**: три поста (диагностика Facebook,
  постбэки любого формата, клоака с журналом причин) и все 91 строка
  видео/статей/шортсов. Всё это — НЕ согласовано; кандидаты в следующие
  месячные выборки, согласуются списком, как C11.
- **Ошибка данных в строке №1**: выдача «CPALENTA→31.08» не состоялась
  (не оплачено, владелец сети молчит — CONTENT-25). Поправить, остальные
  четыре выдачи сходятся с фактом.

## Что делаем

1. Мешок текстов сентября — по согласованным каркасам CONTENT-11, как и
   стояло. Черновые посылы мастера Женя может использовать как подсказку,
   но опора — согласованный каркас.
2. Формат мастера принимаем: выдачи по факту выходов дописываем в его
   строки (первая правка — CPALENTA в строке №1).
3. Темы 23/24 и три новых поста мастера — в следующую месячную выборку
   на согласование списком.
4. Видео/статьи/шортсы мастера — черновой пул под брифы блогерам и статьи
   Жени: перед использованием каждая тема проходит согласование (рамка
   MARKETING-70 §12), как обычно.
