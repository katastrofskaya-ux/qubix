# Qubix — рабочее пространство роста и стратегии

Это личное рабочее пространство Анастасии Войтенко для управления маркетингом,
продажами и ростом Qubix (qubix.pro) на испытательном сроке у YARD TECH S.A.S.
(27.07.2026–26.10.2026).

**Если ты (Claude) открываешь это в новой сессии — читай этот файл первым.** Он
даёт полную картину без пересказа с нуля. Полные материалы по каждой теме — в
`docs/`. Текущий статус задач и прогресс — в `TASKS.md`. Специализированный
агент для этой работы — `.claude/agents/qubix-growth-lead.md`.

Правило поверх всего: **ноль фантазии**. Если чего-то нет в `docs/` или в словах
Анастасии — не выдумывай (цифры, даты, факты о продукте, обещания). Отсутствующая
информация лучше признанного пробела, чем правдоподобная выдумка.

### Правило источника: каждый факт с подписью, откуда он

В любом тексте, который уходит наружу — комментарий в YouTrack, письмо клиенту,
сообщение шефу — рядом с фактом стоит его происхождение. Четыре класса, смешивать
их нельзя:

1. **Слова человека** (клиент, Владимир, admin, Женя) — цитата или пересказ с
   указанием, кто и когда сказал.
2. **Документ** — MARKETING-N, CONTENT-N, файл в `docs/`. Точная цитата плюс номер
   задачи или путь к файлу.
3. **Замер** — то, что реально запрошено. Обязательно назвать **какая база, чей
   контур и на какую дату**. `dash.qubix.capital` — это наш собственный инстанс,
   а не клиентские. Инстансы клиентов стоят on-premise и к нам не приходят вообще,
   поэтому вопрос «сколько клиентов это задело» из ClickHouse не считается никогда —
   только из биллинга/CRM в `admin.qubix.pro`.
4. **Оценка** — прямо назвать оценкой и дать довод, из чего она взята.

Цифра без подписи класса работает как выдумка: она читается шире, чем есть на самом
деле. Прецедент 01.09.2026 — «105 кампаний из 211» ушло в проект комментария шефу
без пометки, что это наш собственный инстанс; прочиталось бы как «по продукту в
целом». Поймала Анастасия, не агент.

Если факта нужного класса нет — писать «не знаю, вот где это лежит», а не подпирать
ближайшим похожим числом.

### Правило краткости (Анастасия, 02.09.2026)

Любой документ, комментарий или сообщение — **максимально понятно, логично и
кратко: чтобы понял даже самый неподготовленный читатель**. Длинные обоснования
и выкладки — в отдельный файл или приложение, не в тело. Если текст можно
сократить вдвое без потери решения — он должен быть сокращён вдвое.

Смежное: в комментарии для шефа и коллег не ссылаться на файлы этого
репозитория (`docs/...`) — у них нет доступа; комментарий самодостаточен,
детали — задачей в Ю-треке.

### Решения Анастасии (02.09.2026) — не переизобретать

Процессные решения принимает Анастасия; агент считает и готовит, но не
придумывает, кто исполнитель, сколько согласований и как устроен процесс.
Действующие решения:

1. Один мешок — одно согласование. Всё пулом, никаких пачек, волн и «частями».
2. Не ужимаемся: бюджет есть, каналы добавляются; просьба к шефу — перенос
   остатка под задачи и активности, каждая трата своим согласованием.
3. Порядок: темы → мешок контента (тексты под ВСЕ темы) → закупка из
   согласованного мешка каналов. Тексты не ждут денег.
4. Ведомость и отчёт месяца — по датам ОПЛАТ, каждая строка со ссылкой на
   задачу-основание (решение владельца 03.09, BRAND-12). Дата выхода —
   рядом справкой; по выходу считается только аналитика каналов.
5. Согласованный с площадкой слот не переносится — в него встаёт тема ротации.
6. Частота 3 — плановая, при возможности закупки; добор другими каналами.
7. Работа живёт в существующих задачах (CONTENT-39, CONTENT-11, CONTENT-4,
   BRAND-12) — новые задачи не плодить; согласовали — апдейтится тело.
8. Роли шефа в документах не подчёркиваем; движение — Анастасия и Женя.
9. Исполнителей (подрядчик роликов и т.п.) называет Анастасия — не подставлять
   имена из догадок.

⚡ **Решение владельца 22.09.2026 по работе с подрядчиками** (дословно, из
переписки): «запрещаю менять условия в худшую для нас сторону, после получения
оплаты», «касается любого», «все правки только через ништяки в нашу сторону».
Пример его же формулировки на просьбу ускориться: «торопишься? да без проблем!
сделай нам тогда +1 ролик бонусом». И рамка по продлениям: решение о длинном
бюджете принимается по метрикам площадки против других, у кого закупаем;
слабые показатели — разовый платёж.

⚠️ Фамилия блогера — **Хоменок** (Антон Хоменок, X6 AGENCY, код KHOMENOK), не
«Хоменюк» (Анастасия, 26.09.2026). Косвенные падежи — с сохранением «о»:
Хоменока, Хоменоку.

Прецедент, из которого это выросло: Хоменок ($8 900, оплата 28.08) снял обзор
на нашем демо вместо своей установки, как требовал бриф; владелец вернул
22.09 «формат не согласован». Любая уступка подрядчику после оплаты готовится
только вместе со встречным требованием.

Перед сдачей любого текста — сверка с этим списком, пункт за пунктом.

### Порядок работы над текстом о фиче (решение владельца, 18.09.2026)

Владелец снял пост об автоправилах (CONTENT-41) как «халтуру» и задал порядок,
обязательный для любого поста/текста/ТЗ про функцию продукта:

1. **Вопросы — задачей в Ю-треке, не в Telegram.** «Для всех вопросов — конечно
   таска в ютреке. И для порядка и для протокола. Не тг». Приходить с вопросом
   ДО текста, а не приносить готовый документ на аппрув.
2. **Порядок ступеней:** три базовых вопроса главному эксперту → его ответы →
   второй круг вопросов с баерами → текст поста → ТЗ. Ни одну ступень не
   перепрыгивать.
3. **Три базовых вопроса дословно:** какую боль клиента решает эта штука; почему
   раньше делали плохо, а теперь хорошо; пример из жизни.
4. **Одиночные тексты на аппрув не принимаются** — только пулом.
5. **Примеры в текстах берутся у баеров, не сочиняются.** Прецедент 18.09:
   пример правила «останови всё, что в минусе третий день» придуман агентом;
   владелец: «абсурд над баингом — ни один баер не оставит на трое суток
   убыточные объявления». Замер по чатам (Маша) подтвердил: реальный горизонт —
   часы (ночной спенд → отключить к утру), и продукт под это и сделан (проверка
   раз в три минуты, паузы 6/12/24 ч и до полуночи в поясе гео).
6. **Механику фичи не описывать по догадке.** Там же: «Правила настраиваются в
   окне правил, а не на любой странице. Ложь в инструкции в текстах самого
   бренда». Кнопка «вставить в редактор» по BRAND-10 живёт внутри редактора
   правил — значит «настраивается с любого экрана» писать нельзя.

Кто главный эксперт по продукту — называет Анастасия (пункт 9 выше про
исполнителей действует и здесь).

### Правило BRAND-9: замечания админа читаются ДО сдачи (Анастасия, 03.09)

В BRAND-9 (team.qubix.capital/issue/BRAND-9, «Конструкция работы»)
админ оставляет замечания по работе агента. **Перед сдачей любой работы —
проверить свежие комментарии этой задачи** (`scripts/youtrack.sh issue
BRAND-9`) и прогнать сдаваемое через них. Прецедент 03.09: четыре
замечания (этаж 1 — CONTENT-39 лишь пример, нужен собственный мастер-план;
деньги — ведомость оплат по датам оплат; прогон — по названным сводам;
порог слабости канала — числом; замер — все ступени воронки, не только
регистрации) пролежали непрочитанными полдня, пока работа сдавалась.

### Правило BRAND-17: своды читаются ДО любой маркетинговой задачи

Введено Анастасией 02.09.2026, обязательно: **прежде чем выполнять любую
маркетинговую/контентную задачу — прочитать BRAND-17
(team.qubix.capital/issue/BRAND-17, «Своды маркетингового агента — тела
скиллов») целиком: тело + ВСЕ комментарии.** Свежая версия свода — самый поздний
комментарий с его именем. Состав на 02.09: #1 дисциплина работы (чек-лист
требований до работы, таблица покрытия перед сдачей, отдельный проход критика),
#2 копирайт, #3 директорский свод, #4 справка по рамкам, #5 humanizer.

Свод #1 ведёт дальше — к конституции маркетинга в Ю-треке, читать целиком перед
работой, которую она задевает: BRAND-1 (платформа бренда, стоп-лист §10,
требования к материалу §11, рамка §12), BRAND-2 (карта болей — **действующий
источник болей; список «8 канонических болей» ниже в этом файле — устаревшая
редакция**), BRAND-3 (ICP), BRAND-4 (ремесло текста), BRAND-5 (прайс),
BRAND-10 (карта возможностей — единственный источник фактов о продукте).
Оговорка из тела BRAND-17: пути файлов и внутренние инструменты конвейера
разработки, упомянутые в сводах #2–#3, этому агенту недоступны — обязательны
правила и порядок, номера задач Ю-трека действительны как есть.

### ⚠️ Переезд конституции в проект BRAND (в ночь на 22.09.2026)

Все постоянные своды перенесены из `MARKETING` в **новый проект `BRAND`**
между 23:59 21.09 и 00:01 22.09. Старые номера работают как редиректы (запрос
`MARKETING-70` отдаёт `BRAND-1`), но в проекте MARKETING их больше нет —
в репозитории номера заменены на новые (337 замен, 22.09). Карта переезда,
проверена запросом по каждому номеру:

| Было | Стало | Что |
|---|---|---|
| MARKETING-70 | BRAND-1 | Платформа бренда |
| MARKETING-68 | BRAND-2 | Карта болей |
| MARKETING-7 | BRAND-3 | Позиционирование и ICP |
| MARKETING-9 | BRAND-4 | Tone of voice |
| MARKETING-5 | BRAND-5 | Прайс и Early Bird |
| MARKETING-97 | BRAND-6 | Автономная космическая станция: образ бренда |
| MARKETING-101 | BRAND-7 | Мир Qubix: план разворота |
| MARKETING-89 | BRAND-8 | Бренд База |
| MARKETING-87 | BRAND-9 | Конструкция работы |
| MARKETING-3 | BRAND-10 | Карта фичей |
| MARKETING-76 | BRAND-12 | Маркетинговая стратегия релиза |
| MARKETING-42 | BRAND-13 | Сравнительная таблица конкурентов |
| MARKETING-72 | BRAND-14 | Чему мы замена |
| MARKETING-41 | BRAND-15 | Affiliate SuperApp — репозиционирование |
| MARKETING-86 | BRAND-17 | Своды маркетингового агента |
| MARKETING-4 | BRAND-18 | Конкурентный анализ |

Отдельно **BRAND-16** — «Фазовая GTM-стратегия», в репозитории по старому
номеру не упоминалась.

**В MARKETING осталась операционка**: ведомости (95, 104), размещения (20, 22,
23, 28, 32, 33, 34, 62, 63), поездки (78, 98), блогер и амбассадоры (77, 82,
83), сайт (75), медиа-карта (10), реферальная программа (13), ресурсы (2).

⚠️ **MARKETING-6 и MARKETING-37 в трекере не существуют** (404, проверено
22.09). Первая была ссылкой на боли в `docs/positioning-and-icp.md` —
исправлена на BRAND-2; вторая помечена как битая в `docs/head-of-growth-hire.md`.

**Борис** — автор BRAND-1 и BRAND-14, в базе знаний его роль не описана.
Спросить у Анастасии, кто это.

### Образ бренда и мир Qubix — прочитано 22.09.2026

Восемь задач BRAND, которых агент раньше не видел, разобраны в
`docs/brand-konstituciya.md`. Оттуда — правила, действующие сразу:

1. ⛔ **Слова образа в продающие тексты не идут.** «Станция», «дроны»,
   «тессеракт», «грани» работают картинкой и сюжетом (BRAND-6 §5). В тексте —
   слова формулы и обычные слова ниши.
2. ⚡ **Facebook — не конкурент, правило «конкурентов не называем» на него не
   действует** (решение владельца 11.09, BRAND-6 §6). О нём говорим прямо по
   имени. Исключение для остальных конкурентов одно — именная сравнительная
   таблица BRAND-13, и только она.
3. ⛔ **Лица бренда на борту нет** (решение владельца 15.09, BRAND-7). Предложение
   Анастасии быть «навигатором Qubix» отклонено: «никаких чужих лиц на борту».
   Герой рекламы — клиент-капитан без лица. Не предлагать это заново.
4. **Мир — «Планеты»**: источники трафика — планеты (Facebook первая),
   гео — регионы планеты и единственный параметр таргетинга в мире. Google и
   TikTok — в материалы только после выпуска клиентам.
5. **Невыпущенное не выдаём за готовое и сроков не называем**, но пометка
   «строится» на лендинге разрешена и обязательна (BRAND-15).
6. **Фраза-ядро на лендинг не выносится** — она для рекламы и креативов; в хиро
   «Ваша команда. Ваш сервер. Ваши данные.» (BRAND-15).
7. **Правило честности сравнений** (BRAND-13): спорную ячейку конкурента
   выставляем в его пользу, наши `✓✓` — только по коду.
8. **Цитируемый замер:** «На сервере за $74 отдаёт 2 000 PWA-витрин в секунду».

⚡ **Бренд-кит — решение владельца 22.09.2026:** бренд-бук есть, бренд-кита
нет, и покупать его за деньги сейчас не будем. Он наполняется постепенно, по
мере производства роликов и контента; визуалы собираем сами, в нужном нам
формате. Заявку на закупку бренд-кита не готовить.

⚡ **Граница работ Анастасия / Женя — решение с владельцем 22.09.2026:** Женя
рерайтер и копирайтер, маркетинговую работу на неё складывать нельзя. Анастасия
решает, о чём говорим и почему (темы, позиционирование, выбор каналов, воронки,
разбор конкурентов); Женя пишет, как это сказано, под уже принятые решения.
Претензия владельца там же: аналитики конкурентов нет — ни соцсетей, ни
контента, «работы маркетолога не видно». Разбор состояния и первый замер SOV —
`drafts/razbor-marketing-na-mne.md` (23.09).

⚠️ Две открытые развилки: аппрув образа владельцем в BRAND-6 не проставлен;
Бренд База по решению владельца 04.09 должна жить на своём QDrive
(`docs.qubix.pro`), а 08.09 материалы положены в Google Drive — уточнить.

## Кто есть кто

- **Анастасия Войтенко** — Consultant по Consulting Services Agreement с YARD
  TECH S.A.S. от 27.07.2026, использует публично титул «CEO» — это НЕ
  корпоративная должность, а courtesy-title для маркетинга/представительства.
  Реальная зона ответственности (Recital A + Schedule 1 договора): продвижение
  и медиа (TG-каналы/чаты, отраслевые сайты и PR, видеоинтеграции с блогерами);
  партнёрства (партнёрские сети, реселлеры, амбассадоры, совместный маркетинг);
  публичное представительство и события (интервью, выступления, конференции,
  собственные события, нетворкинг); управление продажами (воронка от лида до
  оплаты, команда продаж, скрипты и CRM); HR и тимбилдинг; операционное
  управление коммерческой командой и маркетинговым бюджетом. **Продукт,
  разработка и техническая часть — вне её зоны** (у фаундера и dev-команды).
- **Компания** — YARD TECH S.A.S. (Уругвай). Контакт по юридическим/деловым
  вопросам — legal@qubix.pro (Valerey Khaydarov).
- **Владимир** — судя по внутренним документам (MARKETING-38), это, похоже,
  фаундер продукта (в договоре Анастасии Recital A/Cl. 1.1 отдельно
  упоминается «founder and development team» как сторона вне её зоны
  ответственности, отличная от Valerey Khaydarov). Проверить/подтвердить
  напрямую, если важно для конкретного решения.
- **Продукт** — Qubix: self-hosted «Affiliate SuperApp» для медиабайеров
  (Facebook, вертикаль gambling/betting в приоритете) — трекер + PWA-конструктор
  + push + сквозной AI + командный слой (мессенджер, таск-трекер, диск), всё на
  сервере клиента (on-premise). Запуск: 25 июня 2026. Узнаваемость на старте —
  фактически ноль.
- **Инга** — НЕ человек, а sales-бот: на ней висят все карточки лидов в
  проекте SALES, она делает плановые касания (reg_no_license и т.п.), но не
  продаёт. «Отдать очередь Инге» — не решение; живого сейлза в очереди нет
  (подтверждено Анастасией 07.09.2026).
- **Внутренний трекер задач компании** — YouTrack, team.qubix.capital, проекты
  `MARKETING` (документы вида MARKETING-N, на них ссылается вся база знаний
  ниже), `SALES` (лиды/сделки для sales-бота) и `CONTENT` (контент и листинги;
  задачи вида CONTENT-N ведёт **Женя**). ⚠️ MCP к YouTrack НЕ подключён —
  ссылки на MARKETING-N/CONTENT-N в этом репозитории вписаны руками, живого
  чтения задач у агента нет. Не путать одно с другим.

## Испытательный срок — рамка успеха (держать в фокусе всегда)

- Срок: **27.07.2026 – 26.10.2026** (3 месяца), фикс-оплата $5000/мес +
  бонус 3% от Turnover направления.
- **Месяцы 1–2**: развернуть каналы продвижения и партнёрства, запустить
  функцию продаж, собрать базовую метрику CAC.
- **Месяц 3**: стабильный поток платящих клиентов при контролируемом CAC —
  по числу Sales в 3-м месяце определяется и исход испытательного срока, и
  ставка после него (>50 сделок/мес → $8000, 30–50 → $7000, <30 → $7000 и
  компания вправе расторгнуть с уведомлением за 5 дней).
- KPI считает **компания** из своих платёжных/биллинговых систем (не из CRM
  консультанта): число платящих клиентов/мес (Sales), Revenue, CAC.
- Конкретные целевые значения KPI фиксируются обменом e-mail между сторонами
  (Schedule 1 §3) — **проверить, зафиксированы ли они уже**.
- **Ежемесячный письменный отчёт обязателен** (Cl. 2.1.a.ii): услуги за месяц,
  состояние KPI, данные для расчёта Fees. Компания имеет 10 раб. дней на
  возражения.
- **Бюджет требует предварительного письменного одобрения** компании
  (помесячно/поквартально, Cl. 2.3). Без одобрения расходы не возмещаются.
  То же для командировок — **любая** поездка (включая инициированную самим
  консультантом) требует предварительного одобрения цели/бюджета/длительности.
- **Полномочия ограничены** (Cl. 2.2): нельзя подписывать контракты, принимать
  обязательства, давать нестандартные скидки или условия, обещать фичи/сроки
  без письменного одобрения компании для конкретного случая.
- **Публичные материалы** (кейсы, имена клиентов/партнёров, цифры, цены) —
  по NDA Cl. 3.14 требуют письменного одобрения компании. Молчание 3 рабочих
  дня = согласие **только** для не-финансовых и не-партнёрских формулировок;
  имена партнёров/клиентов, цены и любые финансовые/performance-цифры —
  всегда явное одобрение.
- Ограничительные обязательства переживают контракт: non-solicit 24 мес.,
  non-compete 12 мес., non-disclosure деталей сотрудничества 24 мес.,
  неустойка до $100k за нарушение (Section 11).

Полная выжимка операционных условий договора и NDA (без персональных
данных/реквизитов) — `docs/contract-and-nda-summary.md`.

## Позиционирование — читать перед любым внешним текстом

Категория: **Affiliate SuperApp** — «весь офис арбитражной команды на вашем
сервере». Слово в нише свободно, никто себя так не называет.

⚡ **Главная формула бренда — решение владельца 10.09.2026 (BRAND-1 §1),
канонных форм ДВЕ, дословно:** двустишие «Свой сервер — своя ракета! 🚀 /
Тянет от залива до офисного пакета.» (формула и рекламный слоган, где продукт
уже назван рядом) и трёхстрочная с именем «Qubix: / Свой сервер — своя
ракета! 🚀 / Тянет от залива до офисного пакета.» (якорь для холодной
аудитории). Прежняя формула «Qubix — медиабаинг целиком на вашем сервере: от
залива до созвонов и финансов» и слово «медиабаинг» СНЯТЫ тем же решением —
в новые тексты не идут, в старых заменяются. ⚠️ Снято с **продающих текстов и
сайта**, а не из языка вообще: во внутренних задачах, комментариях и разговорах
«медиабаинговые команды» — рабочее слово, подменять его «арбитражными» не надо
(прецедент 22.09, третий случай того же класса, что «круглосуточно» 16.09 и
«не тг» 21.09 — запрет применяется ровно в тех словах, которыми назван). Смежный канон: «Сам Claude — на
всю команду одной подпиской, без наценки сверху»; свой ключ OpenRouter —
отдельный путь отдельной фразой, склейка запрещена; «уровня/аналог Claude»
запрещены. Оба правила вшиты в scripts/check-copy.py.
Формулу «всё в одном» **не используем** (выжженная территория облачных
комбайнов adset.pro/AIO).

Главный столп: **on-premise** — «ваш сервер, ваши данные, ваши связки»
(данные и связки байера никогда не покидают его сервер; после подписки —
доступ в режиме чтения, но не пропадает).

⚠️ **Сайт стоит на августовской редакции — решение владельца от 10.09 до него
не доехало.** Замер живой страницы qubix.pro/ru/ на 21.09.2026:

- «От залива до финансов» — в хиро; хвост формулы, снятой 10.09;
- «media-buying» — 2 раза (подпись хиро и заголовок страницы); английское
  написание снятого «медиабаинга»;
- новой формулы нет вовсе: «своя ракета» — 0 совпадений, «офисного пакета» — 0;
- «Подарок к запуску: 30 дней **Team-тарифа** при регистрации **по
  промокоду**» — ступень называть нельзя (решение владельца 19.08,
  BRAND-5), а подарок выдаётся всем при привязке Telegram (DEV-1883,
  решение владельца 07.08) — это DEV-2582.

Что обновилось: категория «Affiliate SuperApp» и удвоенные цены ($298 / $698 /
$1 398 / $2 398 / $3 998 / $5 998, годовые ×10) — то есть Этап 1 и Этап 3
BRAND-12 выкачены. Прежняя запись про «AI First Tracker» устарела.

⛔ **Задачи на перенос решения 10.09 на сайт НЕТ.** MARKETING-75 — согласование
августовского текста (v6, утверждён 12.08); admin 10.09 дописал туда только
абзац «Договорённость по хиро». Урок для агента: «категория на сайте новая» ≠
«редакция новая» — сверять текст по действующим решениям пословно, а не по
одному признаку. Прецедент 21.09: агент дважды записал сайт в сделанное,
поймала Анастасия.

Полностью: позиционирование по Дэнфорд, ICP, персоны, FAB-матрица,
ценностные столпы, value prop по сегментам, objection handling —
`docs/positioning-and-icp.md`.

### ICP одной строкой
Медиабайер под Facebook (в приоритете gambling/betting), соло или команда
2–30 чел., есть/готов поднять свой VPS, работает с антидетект-браузером,
льёт на PWA/преленды. Квалифицирующий признак: нужен трекер ИЛИ нужно PWA.
НЕ ICP: полностью самописный стек и доволен; не-FB источники как основные;
не готов к self-host.

### 8 канонических болей рынка (⚠️ устаревшая редакция — действующий источник BRAND-2)
1. Прибыльные связки быстро выгорают (данные утекают через облачный трекер).
2. Недолёт событий в Facebook → оптимизация слепнет.
3. Статистика размазана по сервисам.
4. PWA-сервисы съедают прибыль поштучной оплатой инсталлов.
5. Кабинеты банят.
6. Нет автоматизации остановки неэффективных объявлений → ночные сливы.
7. Паразитные комментарии воруют трафик.
8. Домены вылетают из-за антивирусов.

## Тон и стиль — обязательный бриф для любого автора/копирайтера

- **Benefits sell, features tell** — не перечислять фичи, а закрывать боль;
  фича только как доказательство (proof).
- **«Вы»** везде (лендинг, кабинет, доки, реклама), кроме нативной рекламы в
  голосе TG-канала, где органично «ты».
- Никаких «нет/без/никогда/не нужно» внутри продающих фраз — только через
  выгоду. Исключение: прямой ответ «Нет»/«Да» на заданный вопрос — это не
  запретное отрицание.
- Ноль внутреннего технического жаргона; конкурентов в клиентских текстах не
  упоминаем.
- Боль — завуалированно (через вопрос/заботу), не в лоб.
- Фреймворки: PAS (проблема→усиление→решение), BAB (было→стало→мостик),
  FAB (фича→преимущество→выгода), 4P (обещание→картина→доказательство→push).
- Апрув копирайта — железно: показывать точные user-facing строки владельцу
  ДО публикации/записи.
- ⛔ **Часы живой команды и «МСК» во внешних текстах не называем** (решение
  владельца 04.09.2026, предписано админом 09.09, подтверждено Анастасией
  16.09). Запрещены «с 9:00 до 21:00 МСК», «команда на связи с … до …», само
  слово «МСК». **Слово «круглосуточно» разрешено** — канонная строка саппорта
  ровно такая: «бот @qubix_pro_bot отвечает круглосуточно», без времени и без
  часового пояса. Правило вшито в `scripts/check-copy.py` уровнем STOP.
  Прецедент 16.09: агент по горячим следам расширил запрет и на
  «круглосуточно», вырезал его из текстов касания клиентам и собрал на этом
  комментарий в CONTENT-41 против решения админа — расширение снято тем же
  днём. **Урок: запрет применяется ровно в тех словах, которыми он назван; шире
  его не трактуем.**
- ✉️ **Касание клиенту заканчивается вопросом** (Анастасия, 18.09.2026): любое
  личное сообщение клиенту или лиду — в Telegram или почтой — последней
  строкой несёт вопрос, на который человек может ответить одной фразой.
  Утверждение или пожелание в конце не ставим. Приветствие — её каноном:
  «Здравствуйте! Меня зовут Анастасия, я CEO Qubix.» Продление и оплату
  клиент делает сам в кабинете my.qubix.pro — слов «переведу на тариф»,
  «помогу оформить» в текстах нет. Скидка 50% по коду подаётся как стартовое
  условие без публичной даты окончания (решение владельца 07.08, BRAND-5).
Полностью, с примерами before/after и red flags — `docs/tone-of-voice.md`.

**Перед подачей любого внешнего текста на согласование — прогнать через
`scripts/check-copy.py текст.md`** (для поста блогера с ключом `--author-voice`).
Единый свод правил со ссылками на источники — `docs/normativy-vneshnih-tekstov.md`:
там собрано то, что раньше лежало в BRAND-1 §9-10, BRAND-4, MARKETING-13
и MARKETING-2. Прецедент 02.09.2026: пост отклонён владельцем из-за ссылки на
страницу регистрации, та же ошибка нашлась ещё в одиннадцати задачах.

## GTM — три фазы

1. **Фаза 1 (июль, инициализация/land-grab)** — холодный старт, узнаваемость
   ≈ ноль. Таран: Early Bird — Enterprise бесплатно 30 дней всем при
   регистрации до конца июля 2026 (привязано к хайпу ЧМ по футболу).
2. **Фаза 2 (август, узнаваемость)** — сравнение/alternative-контент, social
   proof, доверие.
3. **Фаза 3 (сентябрь+, конверсия/удержание)** — онбординг, кейсы, апселл.

Полностью — `docs/gtm-strategy.md`.

## Каналы продвижения — конкретные площадки и контакты

Основной канал первой волны — Telegram (реклама в чужих каналах/чатах ниши +
рассылки по ботам), плюс YouTube-интеграции с блогерами. Полный
рейтинг площадок с ценами, тирами и контактами продавцов — `docs/media-map.md`.
Сводка по тирам:

- **Tier 1 (брать в первую очередь)**: cparip, .Aff Inside, MoneyBeatsEvil,
  igaming_news (привратник рынка), adhunt.
- **Живые чаты для органики**: gamblingaff (единственный по-настоящему живой,
  50 акт./день), adhuntchat, comments_aff, AFFin_chat.
- **Формат**: нативное видео-демо 15–40 сек (не скринкаст с говорящей
  головой, без ссылок на YouTube) — даёт 2× охват против фото.
- Атрибуция уже реализована технически: `qubix.pro/?r=КОД` → куки → привязка
  при регистрации (подробно `docs/resource-map.md`).

## Конкурентный ландшафт (сжато)

Рынок — три лагеря, никто не закрывает все три: (1) классические трекеры
(Keitaro, Binom, Voluum, RedTrack) — сильный трекинг, нет PWA/push/AI;
(2) PWA-сервисы — сильный конструктор PWA, нет трекера/автоправил, берут
налог за инсталл ($0.02–0.10/шт); (3) облачные комбайны (adset.pro, AIO,
Zeus, Apex) — ближе всего к Qubix, но облако-only и тоже метрят инсталлы.
**Qubix уникален пересечением**: self-hosted + полная воронка + $0/инсталл +
сквозной AI + командный слой.

Известные пробелы (готовый список возражений для продаж/подготовки к
конференциям): нет Google/TikTok интеграции (roadmap), нет BI-конструктора
дашбордов (AIO выкатил такой в июне 2026), нет нативного APK/iOS + MMP
(Adjust/AppsFlyer), узнаваемость с нуля.

Полностью — `docs/competitive-analysis.md`.

### ⚡ Скриптовый слой — ответ на почти любое «а у вас нет фичи X»

Проверено по SDK продукта 31.08.2026. В Qubix есть **User Scripts** — JavaScript,
на котором клиент сам достраивает недостающую логику, и **ИИ-агент в кабинете
пишет этот код за него** (учить программирование не нужно). Что доступно скрипту:

- `` sql`...` `` — read-only доступ к своему ClickHouse (те же таблицы, что видны
  в интерфейсе, под своими правами);
- `ctx.fetch(url, opts)` — исходящий HTTP куда угодно (хост добавляется в allowlist
  в **Система → JavaScript**, там же лимиты на прогон); поддерживает прокси;
- `ctx.state` — память между запусками (защита от дублей);
- `fb.*` — сырой Graph API через браузерный профиль байера, без своего FB-приложения
  и без app review;
- отдельно: **обработчики на путях сайта** (`website_location_save`, Express-style
  `/path/:id` + JS) — срабатывают **в момент запроса**, а не по расписанию.
- Ограничения: синхронный рантайм, нет `async/await`, `setTimeout`, `require`.

**Это философия продукта, а не костыль**: «то, на что у других трекеров уходят
месяцы разработки и всё равно всем не угодишь, мы отдаём самим юзерам» (Владимир).
Qubix продаётся как платформа, внутри которой клиент строит свои сценарии.

⚠️ **Правило для ответов клиентам и для скрипта поддержки**: прежде чем сказать
«такой возможности нет», проверить, решается ли задача через User Scripts или
обработчик сайта. Ответ «фичи нет, в планах» там, где решение уже есть, —
считается неверным ответом (прецедент: вопрос про S2S-постбеки в не-FB источник,
август 2026).


## Продажи / PQL-модель (сжато)

Product-led / low-touch: sales-assist подключается только на PQL (product-
qualified lead — судим по реальному поведению: касание ≥2 value-столпов или
повторная сессия = Hot). Ключевая стадия воронки — бесплатный self-host
инстанс на месяц (человек щупает на своих данных); после установки
поведение внутри инстанса принципиально не отслеживается (on-prem privacy —
это продаваемая ценность, а не ограничение). Полностью — `docs/sales-
playbook.md` и `docs/user-journey-map.md`.

## Ресурсы и ссылки

Все URL, ref-код система, текущая акция, важно: **«Qubix Capital» — чужой
инвестфонд-однофамилец, не наш продукт**, отсекать при мониторинге упоминаний.
Полностью — `docs/resource-map.md`.

## Известные открытые риски / флаги (проверять на актуальность)

- ⚠️ Найден собственный канал-приёмник лидов с FB Lead-формы — «Qubix-lead»
  (id 3772015774, ~204 лида имя+телефон, март 2026, в основном бот-флуд +91);
  пайплайн нашей же Маши извлёк ~196 телефонов в её индекс контактов.
  Публичной утечки не зафиксировано — риск комплаенса 152-ФЗ/GDPR (ПДн в
  базе разведки). Первоисточник: MARKETING-10, раздел 6. Требует эскалации
  команде: чей канал, закрыть, вычистить индекс Маши, фильтр от бот-флуда.
  **Не маркетинговая задача, но обнаружена в ходе аудита — не тянуть.**
- ⚠️ Разрыв позиционирования сайта (см. выше, раздел «Позиционирование»).
- ⚠️ Первый платный тест конверсии дал 0 регистраций/установок (70 кликов →
  3 демо → 0 рег. на $1000, вечер пятницы — маленькая и неудачная по времени
  выборка, но компания сама называет это главным риском). Не разворачивать
  бюджет на полную мощность, пока не прогнан нормальный тест в будни с
  замером каждого шага воронки. Подробно — `docs/head-of-growth-hire.md`.
- ✅ Флаг про Head of Growth СНЯТ 07.09.2026: со слов Анастасии, Head of
  Growth — это она сама и есть, шеф брал её именно под эту роль
  (`docs/head-of-growth-hire.md` описывает её же позицию, а не параллельный
  найм). Конфликта периметра нет.

## Интеграции (MCP) — что подключать дальше

Сейчас весь контекст в этом репозитории — статический (снимок на 28.07.2026).
План: по мере готовности подключать MCP-серверы, чтобы агент видел живые
данные, а не только докcы. Кандидаты:

- **YouTrack (team.qubix.capital)** — проекты `MARKETING` и `SALES`. Даст
  прямое чтение/обновление задач MARKETING-N и лидов в SALES вместо ручной
  сверки с этим репозиторием.
- **Бэкенд/аналитика Qubix (my.qubix.pro, admin.qubix.pro)** — живые цифры
  Sales/Revenue/CAC по каналам вместо ожидания ежемесячного отчёта компании.
- Другое по мере необходимости (Telegram Bot API для рекламных размещений,
  биллинг и т.д.) — добавлять сюда по факту подключения.

**Правило для агента**: если в сессии подключён MCP-инструмент к одной из
этих систем — доверять живым данным из него больше, чем статике в `docs/`
(которая является историческим срезом на момент написания, не live-источником).
Когда подключаешь новый MCP-сервер, впиши его сюда с датой и что именно он даёт,
чтобы это не потерялось между сессиями.

### Статус: Qubix MCP — ПОДКЛЮЧЁН И РАБОТАЕТ (31.08.2026)

Сервер `qubix` (`https://dash.qubix.capital/api/mcp`) отвечает из сессии.
Токен подхватывается из переменной окружения `QUBIX_API_TOKEN`, заданной в
Settings/Secrets окружения — **в файлах репозитория токена нет**. Сетевая
блокировка (403/`connect_rejected`), которая мешала 28.07, снята.

Под каким доступом работает агент (`mcp_whoami`): `anastasiavoitenko@qubix.pro`,
user_id 991000, team_id 1, **is_admin: true** — то есть доступны в том числе
write-действия (управление командой/юзерами, ставки и статусы объявлений,
правила Бритвы, PWA-публикация, push-рассылки, покупка доменов, задачи и диск).

⚠️ **Правило безопасности для агента**: это боевой админский доступ к продукту,
а не песочница. Любой write-вызов (`call` с `confirm=true`) — только по явной
просьбе Анастасии в этой сессии, с проговоренным вслух эффектом. Всё остальное —
только чтение. Отдельно помнить Cl. 2.2 договора: полномочия консультанта не
включают обязательства перед клиентами — рассылка push'ей клиентам или правки
чужих настроек могут выйти за периметр.

Как этим пользоваться (интерфейс намеренно узкий):
- `search(query, kind?)` — единая точка входа; напрямую перечислено только 7
  discovery-инструментов, остальные ~100 находятся через неё
  (`kind`: tool | skill | playbook | schema | doc).
- `call(name, args, confirm?)` — диспетчер для найденных инструментов.
- `query_ch` — ClickHouse-аналитика, `fetch_doc` — полный текст статьи доков,
  `read_skill` / `run_playbook` — внутренние гайды и пошаговые сценарии.

Что это даёт по договору: живые Sales/Revenue/CAC и поведение внутри продукта —
вместо ожидания ежемесячного отчёта компании. **Но KPI по договору считает
компания из своих платёжных систем** — цифры отсюда используем для управления
и подготовки, а в отчёте сверяемся с компанией, а не подменяем её источник.

- Рекомендация в силе: токен дважды был вставлен в чат открытым текстом —
  перевыпустить его в кабинете Qubix и обновить переменную окружения, чтобы
  боевой токен не совпадал с тем, что попал в переписку.

### Статус: Masha MCP — ПОДКЛЮЧЁН И РАБОТАЕТ (31.08.2026)

Сервер `masha` (`https://masha.qubix.pro/`) отвечает; токен из переменной
окружения `MASHA_API_TOKEN` (в репозитории его нет).

Что это на самом деле: **read-only база Telegram-разведки** (ClickHouse `tg`) —
сообщения, посты, комменты, медиа с OCR и транскриптами голосовых, профили,
контакты, векторный поиск. Это ровно тот инструмент, которым нужно проверять
`docs/media-map.md` вместо доверия статике.

Инструменты:
- `semantic_search` — поиск по смыслу; `read` / `get_context` / `get_post` /
  `get_messages` — дочитать исходники.
- `analyst_sql` — только один SELECT/WITH, для цифр и агрегатов (не для поиска
  контента); `describe_schema` — карта базы, смотреть первой.
- `deep_dive(subject)` — батарея углов по бренду/персоне (риск, скам, выплаты,
  репутация) одним вызовом; звать в начале разбора любого контрагента.
- `find_contacts` — @-хэндлы/почты менеджеров рекламы для аутрича.
- `find_person` — поиск людей по карточкам личности.

Прикладная ценность прямо сейчас:
1. **Проверять площадки перед оплатой.** `v_channel_metrics` даёт живое ядро:
   `avg_dau`, `stickiness`, `core_regulars`, `top10_share` (0.9 = «одни и те же»),
   `core_per_1k_subs` — детектор «большой, но мёртвый». Это прямой ответ на риск
   слить $10k/мес в TG вслепую.
2. **Проверять рекламодателей и контрагентов** (`deep_dive`) до сделки — под
   ограничение полномочий Cl. 2.2 это дешевле, чем разбираться постфактум.
3. **Share-of-voice и упоминания конкурентов** (Keitaro, Binom, adset.pro и др.)
   — фактура для `docs/competitive-analysis.md` вместо оценок на глаз.
4. **Контакты продавцов рекламы** (`find_contacts`) вместо ручного сбора.

Ловушки, которые база сама называет (не забывать): каналы идентифицировать по
`channel_id`, а не по `username` — ⛔ прецедент DEV-2627 (10.09): сверка
покрытия мешка по имени дала «17 из 33» при реальных «31 из 33», у 65 из 228
каналов имя пусто; любую сверку покрытия делать по channel_id/инвайт-ссылке и
перед словом «отсутствует» проверять противоположное вторым запросом; `channels` читать с `FINAL`; `is_ad_heuristic`
груба; `last_post` сломан для мегагрупп (живость чатов смотреть по `mau_30d`);
видео не индексировано; **покрытие = только те каналы, куда вступил парсер —
чего нет в базе, то «не отслеживается», а не «не существует»** (это прямо
ложится на правило «ноль фантазии»).

- Рекомендация в силе: токен Masha тоже был в чате открытым текстом —
  перевыпустить и обновить переменную окружения.

### Статус: YouTrack — РАБОТАЕТ через REST API (01.09.2026)

Задачи `MARKETING-N` / `CONTENT-N` живут в YouTrack на team.qubix.capital.
**MCP-сервера для него нет**, но доступ есть: домен разрешён в сетевой политике
окружения, токен лежит в переменной `YOUTRACK_API_TOKEN`. Читать так:

```
scripts/youtrack.sh issue MARKETING-83     # задача + комментарии
scripts/youtrack.sh list CONTENT 30        # последние задачи проекта
scripts/youtrack.sh search "листинг"       # поиск по тексту
```

Под капотом — обычный `curl` с `Authorization: Bearer $YOUTRACK_API_TOKEN`.
Только чтение; на запись задачи не трогаем без явной просьбы.
⚠️ Правило записи (Анастасия, 14.09.2026): даже при явной просьбе
«отпишись/закоммить» — сначала показать ей ТОЧНЫЙ текст коммента, в
трекер отправлять только после её «да» на этот текст. Слова «отпишись»,
«отправь», «поставь» без показанного текста разрешением НЕ считаются.

⛔ **Что считается разрешением (Анастасия, 16.09.2026).** Только явное
«да», «отправляй», «шли», «публикуй». Слова **«ну», «ок», «угу», «давай»,
«ага», «хорошо» разрешением НЕ считаются** — при них агент переспрашивает
одной строкой: «отправлять?». Прецедент 16.09: на показанный текст
комментария Анастасия ответила «ну», агент прочитал это как «да» и
отправил в CONTENT-41 без разрешения. Удалить не смог — токен YouTrack
даёт правку комментария (POST на /comments/{id} → 200), но не удаление
(DELETE → 403); удаление возможно только руками из интерфейса.
Прецедент 14.09: коммент в CONTENT-41 ушёл без показа — в нём оказалась
спорная трактовка по имени «Britva» (раздел в продукте так называется,
публичные доки тоже — «внутреннее» имя уже видно клиентам).

⚠️ Урок 01.09: агент дважды заявил, что доступа нет, потому что искал
переменную не по тому имени и обрезал вывод `env`. **Прежде чем сказать
«доступа нет» — проверить `env | grep -i <сервис>` целиком и попробовать
запрос.** Это тот же класс ошибки, что и ответ клиенту «такой возможности нет».

Внутренний таск-менеджер продукта Qubix (проекты MAR/SAL/DEV, таблицы
`tasks`/`task_projects` в ClickHouse) — **другая система**, маркетинговых задач
в ней не ведут.

### Статус: admin.qubix.pro — РАБОТАЕТ через curl с кукой (02.09.2026)

Домен разрешён в сетевой политике окружения (добавлен Анастасией 02.09).
MCP-сервера нет; доступ — обычный `curl` к `/api/admin/v1/...` с кукой сессии
админки (`qubix_admin_gate` + `qubix_admin_jwt`; jwt живёт до ~08.11.2026).
Кука бралась из браузера (DevTools → Network → Copy as cURL) и 02.09 светилась
в чате открытым текстом — **после работы перелогиниться в админке**; правильное
хранение — переменная окружения `ADMIN_QUBIX_COOKIE` (на 02.09 ещё не заведена).

Проверенные эндпоинты: `/me`, `/customers?limit=&offset=` (+`/customers/filters`),
`/promo-codes`. Это источник класса «замер» для правила источника: регистрации,
`referred_by_code`, `billing_state`, `plan_code`, `paid_until` — то есть счёт
регистраций и оплат по каналам. KPI по договору всё равно считает компания.

⚠️ Ловушка при замере по кодам: у одного канала бывает два-три кода — старый
реф-код без подарка (discount 0, bonus 0: CPARIP_old, ADHUNT, CPAGRAM,
AFFINSIDE, IGAMINGNEWS…) и новый подарочный (99%/30 дней на pwa_team: ADHUNT1,
CPAGRAM1, AFF_INSIDE, IGAMING_NEWS…). Регистрации приходят на оба — при счёте
канала суммировать все его варианты.

⚠️ Правило безопасности то же, что для Qubix MCP: это боевой доступ — только
чтение; любые write-вызовы к админке — только по явной просьбе Анастасии.

### Статус: Airtable MCP — ПОДКЛЮЧЁН, аккаунт был пустой (31.08.2026)

Воркспейс один — «My First Workspace» (`wspfIIYvTyFohFyhe`), права owner, баз
на момент подключения не было ни одной. MCP отдаёт почти весь стек: базы,
таблицы, поля, записи, схемы, views (только чтение), интерфейсы и страницы,
автоматизации, комментарии, формы, плюс `revert_action` (откат последнего
действия).

Создана **учебная** база `УЧЕБНАЯ — Медиазакупки Qubix` (`app66aVkGaNY9c2w5`)
— чтобы разобраться, как инструмент устроен. Две связанные таблицы:
«Площадки» (16 записей — реальные данные из `docs/media-map.md`) и
«Размещения» (6 демо-строк).

В базе 4 таблицы: «Площадки» (16, из `docs/media-map.md`), «Размещения»
(6 демо-строк), «Листинги» (8 — реальные, из задач CONTENT, см. ниже) и
«Задачи» (8, зеркало CONTENT-задач со связями на остальные три таблицы).

**Листинги на сентябрь 2026** (завела Женя 31.08.2026, из ссылок YouTrack):
Partnerkin (CONTENT-35), Traffic Mafia (CONTENT-33), Traffnews (CONTENT-32),
TraffHub (CONTENT-27), ProTraffic (CONTENT-29, +обзор), AffTimes (CONTENT-30,
+обзор), FB killa (CONTENT-26, +обзор), Partneroff (CONTENT-36). Известны
только названия и номера задач — статусы, цены и даты в YouTrack, не у агента.
Отдельно: **FB killa — это и листинг, и TG-канал `fb_killa` (T2) из медиа-карты**,
один контрагент на два канала сбыта — есть смысл торговаться пакетом.

⚠️ **Цифры воронки в «Размещениях» (клики/регистрации/оплаты) ВЫДУМАНЫ** для
демонстрации формул и rollup'ов — это помечено в описаниях полей. Не
цитировать их никуда как факт. Реальные площадки, тиры, прайсы и контакты —
настоящие, из медиа-карты.

Ограничение API: **views (Grid/Kanban/Calendar) через MCP не создаются**,
только читаются — их делают руками в интерфейсе Airtable.

Открытый вопрос: под какую задачу Airtable подключался изначально — неизвестно.
Воронка продаж живёт в YouTrack `SALES`, дублировать её здесь не надо.

### ⚠️ Возраст статики (проверять при каждом заходе)

Документы в `docs/` — снимок на 27–30.07.2026. Сегодня 31.08.2026, то есть
прошёл **месяц с лишним, а это уже 2-й месяц испытательного срока из трёх**.
Цены площадок, состав чатов, статус лендинга и прогресс по `TASKS.md` могли
измениться. Теперь есть живые источники (MCP выше) — при расхождении верить
им, а не `docs/`, и обновлять документы по факту проверки.

## Планирование: как устроен день

`PLAN.md` — план Анастасии в разбираемом формате: цель по договору, окно набора
регистраций, фокус недели, срочное, чего ждём от других, календарь, ежемесячные
обязательства. Правится руками, это её план.

`BUDGET.md` — ручная книга бюджета по месяцам (оплачено / одобрено / свободно).

Команды:
```
scripts/day.py            # разбор дня: горит, жду других, календарь, лиды, бюджет
scripts/day.py --week     # плюс вся неделя и задачи без срока
scripts/leads.py --hours 1  # новые лиды и обращения за час
scripts/touch.py          # кого касаться: застряли / истекают / истекли
scripts/touch.py --letters    # готовые тексты + ссылки на карточки
scripts/touch.py --csv с.csv  # то же выгрузкой для рассылки
```

**Кого касаться (создан 14.09.2026):** `scripts/touch.py` — три сегмента из
админки: застрявшие между регистрацией и установкой, истекающие подписки
(разговор нужен ДО даты) и истёкшие за последние две недели. Показывает код,
скидку по коду и есть ли привязанный Telegram; тестовые и свои аккаунты
отсеиваются (списки `TEST_DOMAINS`, `TEST_NAMES`, `OURS`, `OWN_EMAILS` —
пополнять по мере появления новых технических записей). Нужна переменная
`ADMIN_QUBIX_COOKIE`. Только чтение: скрипт готовит списки, не отправляет.
Ключ `--letters` выдаёт по каждому человеку готовое касание: канал (ссылка на
Telegram или почта), ссылку на его карточку в SALES (ищется по почте) и на
карточку клиента в админке, текст с подставленной датой и готовую строку
отметки для карточки. Тексты — `drafts/kasanie-istekayushchim.md`, при правке
менять в обоих местах (в скрипте `LETTER_EXPIRING` / `LETTER_EXPIRED`).

⚠️ **Правило проверки перед словом «не сделано» (Анастасия, 16.09.2026).**
Прежде чем сказать, что работа не отправлена или не сделана, агент обязан
проверить **тело задачи И все комментарии**, а не только комментарии, и пройти
по **всем** задачам, а не по выбранным наугад. Про **Telegram, почту и личные
переписки агент не утверждает ничего** — они ему не видны; допустимая
формулировка только «в трекере следов нет, про переписку не знаю».
Прецедент 16.09: агент посмотрел комментарии шести задач и объявил семь работ
черновиками — половина была отправлена телом задачи или в Telegram, и
Анастасия перепроверяла за агентом.

⛔ Прецедент 21.09, второй за день: в SUPPORT-27 агент прочитал записи 1–4, 19
и 30–42, а середину пропустил — и ровно там, записью #28 от 07.09, стояло
«@dispatcher я написала сама руками». На этом были построены два ложных вывода:
«клиенту не передали ответ» и «клиент идёт по прежнему совету». **Комментарии
читаются подряд, все, а не начало и хвост.** Отдельно: раз Анастасия ведёт
клиента вручную в Telegram, о содержании этой переписки агент не утверждает
ничего.

⛔ Прецедент 21.09: агент поставил SUPPORT-26 в «горит — дожать», хотя ответ
клиенту согласован владельцем и **отправлен ботом в тот же день 01.09**, третьим
комментарием задачи. Две ошибки разом: «нет исполнителя» прочитано как «работа не
сделана» (стадию просто не переключили), и заявлено «клиент не ответил» — а это
почта и бот, которых агент не видит. **Отсутствие исполнителя и незакрытая стадия
работой не считаются; перед словом «висит» читать ВСЕ комментарии до последнего.**

⛔ Прецедент 23.09, третий: в BRAND-18 агент описал витрину каталогов как «кто
стоит в тех же категориях, что и мы» — по телу CONTENT-3, не открыв её 23
комментария и задачи площадок. А там решение Анастасии 04.09: «либо суперапп,
либо трекер + PWA, не хотят — как хотят». На семи площадках из восьми мы в
собственной категории SuperApp, рядом никого; Traffnews, Traffic Mafia и grey
hunter — наш отказ, а не «одобрено, не оплачено», как три недели стояло в
BUDGET.md. **Категория закупки — решение, оно в комментариях, не в теле.** Там же, в CONTENT-64: пакет SYSBITRAZH выходит сразу в два канала — ARMBITRAZH второй, и агент записал его в «не покупаем».
И второе: работа сдавалась без прогона через BRAND-17 (свод №1) — это и
позволило ошибке дойти до Анастасии.

⚡ **Периметр агентов с 24.09.2026 (решение Анастасии):** копирайтинг и
рекламные тексты ведёт отдельный агент; этот — верхнеуровневый маркетинг:
стратегия, конкуренты, замеры, бюджет, договор и финансы, трекер, дневной
ритм, DEV, подрядчики. Тексты сюда приходят только на ворота (своды,
стоп-листы, критик). Восемь обязательных вопросов «думать наперёд» перед
любой сдачей — в `.claude/agents/qubix-growth-lead.md`, выведены из ошибок
24.09 (адрес поменян в одном файле из четырёх; «сделал» до проверки; ноль по
поиску; жаргон «адсет» в счёте; фраза, компрометирующая Анастасию; обрезанная
дельта «без бубна»).

⛔ Прецедент 24.09: в BRAND-18 агент записал «Qubix на YouTube — 0 роликов»
по результатам поиска YouTube. Поймала Анастасия: у Хоменока вышли два
преролла (06.09 и 13.09, ссылки в MARKETING-83), а преролл внутри чужого
выпуска поиском не находится в принципе. **«Не нашёл в поиске» ≠ «нет».
Перед нулём по собственному бренду — сверка с трекером (MARKETING-83,
VYHODY.md, задачи размещений), и ноль пишется только со словами «в поиске не
найдено», а не как факт.** Тот же класс, что «доступа нет» 01.09 и «фичи
нет» в ответах клиентам.

⛔ Повтор 26.09: агент написал Анастасии «постов Иванова о Qubix с 29.07 — ни
одного», хотя в ответе его же запроса к базе стоял анонс 01.09 в @pacan
(канал Иванова, в базе id 2271615832, имя лежит в массиве `usernames`, а не в
`username`). Поймала Анастасия («пост он точно выпускал»). **Каждую строку
выдачи дочитать до конца, прежде чем писать ноль; канал искать по
`usernames`, а не по `username`.** Женя Иванов = @dumay = tg 6471648282 =
клиент №96 (SALES-100); «Женя» без фамилии в команде — это Евгения, копирайтер.

⛔ Прецедент 23.09, четвёртый — документы по договору (FIN-13, инвойсы). Три
ошибки подряд, все пойманы владельцем: (1) задача написана наоборот — «компания
выставляет письмо на бланке с визой», хотя инвойсы выставляет консультант
(п. 2.5), бланков и подписей на инвойсах нет; (2) в инвойс вписан «кошелёк из
договора», не открыв договор — Designated Wallet п. 4.8 совпал с FIN-2/7 случайно;
(3) в инвойс поставлена ссылка на п. 4.5(a) «в первые 10 дней следующего месяца»,
а обе оплаты были авансами раньше срока — документ сам показывал нарушение.
**Правило: каждое утверждение в документе по договору — сверка по номеру пункта
в самом договоре, не по выжимке; перед сдачей отдельный вопрос «какая строка
может быть обращена против Анастасии».** Владелец затем (00:11, 24.09) прогнал
инвойсы сам и нашёл ещё пять: слово «аванс», которого нет в договоре; 3 900 за
август без расшифровки (5 000 минус зачёт подотчёта 1 100 — расчёт Kit 27.08);
почта @qubix.pro вместо контакта по п. 9.2; письмо-подтверждение должно быть в
форме ведомости п. 4.5(a) с адресом отправителя; FIN-12 на новый кошелёк без
процедуры п. 4.8. **Его пять пунктов — чек-лист для любого документа по
договору: слова только из договора · каждая сумма с расшифровкой до чека ·
реквизиты сторон из договора · форма подтверждения из договора · кошелёк по
п. 4.8.** Порядок подтверждения оплат по слову
владельца 23.09: консультант выставляет инвойсы (белый лист A4, PDF, номер =
дата + порядковый номер, без подписей) → пишет письмо в компанию с просьбой
подтвердить оплаты по инвойсам → ответ компании «да, подтверждаем» (.eml) и есть
подтверждение. Только оплаченные выплаты; неоплаченное в пакет не идёт. ⛔ **Задним числом — никогда** (admin FIN-13,
23.09 21:34): PDF несёт CreationDate/Creator, проверяющий читает их первыми;
инвойс с датой 27.07, созданный 23.09, читается как подделка и переводит дело в
расследование. Дата выставления — сегодняшняя; период услуг и дата уже
полученного платежа с хешем — строками внутри. Личные
данные (адрес, паспорт) в репозиторий не кладутся — генератор
`drafts/invoice-gen.py` читает адрес из gitignored-файла.

⚡ **Постоянная обязанность (владелец, 24.09 00:48; Анастасия — «сделай пометку»):
инвойс компании выставляется ДО каждой выплаты, самой Анастасией, почтой.**
«По договору ты и должна присылать инвойсы, сама и в срок. Это твоя же защита.
И это не забота компании» (п. 2.5: «The Consultant invoices the Company for the
Fees»). Порядок каждый месяц: до даты выплаты — `python3 drafts/invoice-gen.py`
(дата выставления = сегодня, период услуг = месяц, зачёты — строкой по расчёту
компании) → PDF → письмо с Katastrofskaya@gmail.com на legal@qubix.pro (п. 9.1
— только почта) → копия в задачу FIN на эту выплату. Агент напоминает об этом в
утреннем разборе (`scripts/day.py`) за 3 дня до срока по п. 4.5(a) — первые 10
дней следующего месяца — и в конце каждого месяца; при взгляде на любую задачу
FIN «выплата» без приложенного инвойса — поднимать сразу. Сделано: инвойсы 2026-09-24-1/-2 (июль–август,
постфактум, FIN-13) и 2026-09-24-3 (сентябрь, 4 319, до выплаты, в FIN-12) —
все отправлены 24.09. Следующий: за октябрь, 30–31.10, до выплаты.

⛔ Прецедент 24–25.09: агент объявил, что Маша «теряет посты нашего канала» —
вывел это из слов «вышел кружок» и отсутствия кружка в базе, а сам канал открыть
не мог (t.me был закрыт сетевой политикой). По живой странице 25.09 всё
наоборот: в Маше есть все посты и кружки, включая три от 12.09, которые сняты
с канала («Post not found»); не снимаются только просмотры (DEV-2645).
**Если второй источник недоступен — писать «проверить не могу», а не выводить
дефект из одного источника. Дефект объявляется только после сверки двух.**

⛔ Прецедент 25.09: YouTrack по умолчанию отдаёт **не больше 42 комментариев**, а
`scripts/youtrack.sh`, `svodka.py`, `vedomost.py` и `front_board.py` лимит не снимали.
В длинных задачах агент видел только первые 42 записи (SALES-160 — 231, SUPPORT-27 — 54,
BRAND-12 — 43), и сводка не видела свежих отправок. Починено 25.09 (`$top=5000`).
**Если число комментариев ровно 42 — это обрезка, а не конец ленты.**

**Сводка движения (создана 16.09.2026):** `scripts/svodka.py` — ровно против
этой ошибки. Две части: (1) движение по всем проектам за период, с разбором,
что изменилось — тело или комментарий, кто и когда; (2) судьба каждого
черновика из `drafts/` — ушёл в трекер целиком, частью или следов нет.
Ключи: `--hours N` (окно движения), `--days N` (свежесть черновиков),
`--drafts` (только черновики).

Чтобы вторая часть работала, **каждый черновик объявляет адрес строкой в
начале файла**: `Куда: CONTENT-3` — проверяется поиском по этой задаче, либо
`Куда: Telegram — Ник` — внешний канал, скрипт честно пишет «проверить
нельзя». Строку ставит агент, когда создаёт черновик.

⚠️ Отвечать на вопрос «что не отправлено» разрешено ТОЛЬКО выводом
`scripts/svodka.py`. Руками, с отсечкой времени на глаз, — нельзя: 16.09 агент
так промахнулся на час и объявил неотправленным комментарий, который стоял в
задаче. И **каждый черновик сохраняется файлом в `drafts/` со строкой «Куда:»**
— то, что показано только в чате, скрипт отследить не может.

Сверка идёт по нескольким опорным фразам, а не по одной: отправляя, человек
часто срезает хвост, и один пробник дал бы ложное «не найдено». Отсюда три
исхода — «целиком», «частично N из M», «следов нет».

**Почта клиентам (создан 16.09.2026):** `scripts/mail.py` — читает папку
«Отправленные» рабочего ящика `anastasiavoitenko@qubix.pro` на `mail.qubix.pro`
по IMAP, сопоставляет адресатов с клиентами из админки и карточками SALES,
по каждому письму выдаёт готовую строку отметки. Ключи: `--days N`, `--new`
(только то, чего не было в прошлом запуске), `--all` (включая адресатов вне
клиентской базы). Состояние — `data/mail-state.json`, в репозиторий не идёт.
Нужна переменная `QUBIX_MAIL_PASSWORD`. Только чтение: ничего не отправляет
и в трекер не пишет — правило показа текста перед записью действует и здесь.

⚠️ На 16.09 скрипт **не проверен на живом сервере**: `mail.qubix.pro` не
принимает подключения ни с ноутбука Анастасии (Mail.app не отправляет,
таймаут), ни из окружения агента — таймаут на 993, 143, 587, 465 при живом
DNS. Это не пароль и не окружение, а сторона сервера; вопрос у Ника.
Пароль ящика 16.09 засветился в чате открытым текстом и сменён.

**Голоса клиентов (создан 21.09.2026, конструкция Анастасии):** три ступени,
и мусор до трекера не доходит.

1. **Чат — приёмник сырья.** Анастасия (и кто ещё она решит) кидает туда всё,
   что сказали пользователи: пожелания, претензии, «а можно ли», причины ухода.
   Ничего не оформляется, порог нулевой.
2. **`GOLOSA.md` — свод к созвону.** Агент раскладывает сырьё по строкам
   `дата · кто · «дословно» · тема · статус` и до созвона проставляет проверку:
   `есть` (работает уже — ответить человеку со ссылкой), `доработать`
   (кандидат в разработку), `не делаем` (с доводом).
3. **Ю-трек — только то, что на созвоне решили делать.** Задача заводится под
   доработку, а не под каждое пожелание. Весь остальной разбор остаётся в своде.

⚠️ Прежде чем записать «фичи нет» — обязательная проверка по скриптовому слою и
BRAND-10 (прецедент MARKETING-84: бот ответил клиенту «менять бюджет Qubix не
умеет», хотя умеет скриптом, и рецепт лежит в примерах продукта).

⚠️ Агент не видит Telegram. Если приёмник живёт там, заметки попадают в свод
только когда Анастасия перекидывает их сюда пачкой; сам агент их не подтянет.

⛔ Урок 21.09: агент растянул запрет владельца «не тг» (сказанный про вопросы к
нему) на её собственный рабочий приёмник и отговаривал заводить чат. Это тот же
класс ошибки, что прецедент 16.09 с «круглосуточно»: **запрет применяется ровно
в тех словах, которыми назван, шире не трактуем.**

✅ **UTM починились — проверено 21.09.2026.** Живой запрос
`qubix.pro/?r=ADHUNT1&utm_source=test&utm_medium=seed&utm_campaign=sep` даёт
302 → `/?utm_*` → 302 → `/en/?utm_*` → 200: **все метки переживают редирект**,
реф-код уходит в куку `qubix_ref` на 30 дней. Это выкачена DEV-2508.
⛔ Прежнее требование давать площадкам ссылку с `/ru/` **СНЯТО** — оно было
обходом потери меток и больше не нужно. Если встретится в старых черновиках
или задачах — убирать.

**Замер по выходам (создан 21.09.2026):** `scripts/zamer.py` — считает воронку
по каждому размещению вместо ручного счёта. Конструкция утверждена владельцем
04.09 (BRAND-9): регистрация → выдача лицензии → **сервер отчитался**
(`last_seen` в карточке лицензии) → покупка (`payments` со статусом `paid` и
ненулевой суммой), окно 7 дней после выхода, цена ступени против лестницы
$110 / $220 / $379. Выдаёт готовую таблицу для тела задачи-реестра плюс
сравнение кругов по одному каналу.

Список выходов ведётся руками в `VYHODY.md`: `дата · канал · круг · задача ·
сумма · коды`. Строка заводится по факту ВЫХОДА, не оплаты; оплаченное без
подтверждённого выхода лежит отдельным разделом и в замер не идёт.

⚠️ Ловушка, из-за которой скрипт и появился (прецедент 21.09): у каналов есть
старые коды без суффикса (ADHUNT, AFFINSIDE, CPAGRAM, IGAMINGNEWS), по которым
регистрации приходили ещё в июльский круг. Счёт накопительно по коду
приписывает июльскую отдачу сентябрьской закупке — **считать только окном от
даты выхода**. Ключ `--all` даёт накопительный счёт и нужен для другого:
понять, сколько канал принёс за всё время.

Ключи: `--window N`, `--all`, `--voronka`, `--cache` (клиенты из прошлого
прогона, без 180 запросов к админке). Нужна `ADMIN_QUBIX_COOKIE`. Только
чтение; кэш `data/zamer-clients.json` в репозиторий не идёт.

**Сверка ведомости (создана 15.09.2026):** `scripts/vedomost.py` — три
проверки: (1) оплата есть в трекере, а строки в `BUDGET.md` нет — ищет ссылки
на транзакции в телах и комментариях CONTENT/MARKETING и сверяет по началу
хеша; (2) оплачено, а факта выхода нет — по задаче приёмки листингов
CONTENT-40, где площадка → ссылка на карточку; (3) рамка месяца против
$30 000. Вшита в `scripts/day.py`, поэтому пропущенная оплата всплывает на
утреннем разборе сама. Только чтение — правки в `BUDGET.md` руками.

⚠️ Правило замера, из которого это выросло: **пока у оплаченного размещения
нет факта выхода, ноль регистраций по его коду в замер не берётся** — ноль
означает «ещё не вышло», а не «не сработало».

`BUDGET.md` ведётся по решению владельца 03.09: строка на каждый платёж
площадке — дата оплаты · сумма · статус · что · задача-основание · tx. Внутренние
переводы (Kit → Евгения под пул оплат) тратой не считаются и лежат в разделе
«Транзакции вне ведомости», иначе сверка поднимает их как пропущенные.
Остаток месяца не переносится (решение владельца 15.09). Ведомость месяца
дублируется задачей в Ю-треке — первая заведена по сентябрю.

**Карта фронта (создана 07.09.2026):** `scripts/front.py` — вся картина
одним экраном, светофоры по блокам: продажи (админка), очередь лидов
(YouTrack), наш канал (Маша), закупка и бюджет (BUDGET.md), финансы (FIN),
плюс ручные блоки из `FRONT.md` (подрядчики, согласования у шефа, риски —
краснеют от возраста записи). Ручная половина правится в FRONT.md руками
или по явной просьбе; утренний разбор 08:00 начинается с этой карты.
Для блока продаж нужна переменная окружения `ADMIN_QUBIX_COOKIE` — пока не
заведена, блок честно пишет «нет данных».

**Живая доска (создана 07.09.2026):**
https://claude.ai/code/artifact/8775441a-4e99-4200-a2fa-b9a3badd4593 —
визуальная версия карты фронта: блоки со светофорами, все открытые задачи
Ю-трека по блокам (ссылками), отмечаемые галочки (переживают обновления).
Устроено так: `scripts/front_board.py --out <dir>` собирает JSON-документы
(те же источники, что front.py, плюс списки #Unresolved-задач по проектам),
дальше они заливаются инструментом Artifact (write_db, batch) в базу доски:
файлы `blocks/*.json` → коллекция `blocks`, `meta.json` → документ
`board/meta`. Галочки живут в коллекции `checks` — её при синке НЕ трогать.
Блок с недоступным источником скрипт пропускает (на доске остаётся прошлый
срез со своей датой «срез DD.MM»). Отдельный блок `ceo` («Взгляд СЕО: где проседает») — ручная аналитика
Клода, скрипт его не трогает; обновлять по слову «обнови сводку СЕО».
Синк вшит в утренний триггер (08:00 МСК)
и в почасовую проверку лидов; в любой момент — по слову «обнови доску».
Прямого вебхука из YouTrack нет — «живость» это часовой шаг, не секунды.

**Расписание (создано 02.09.2026 по просьбе Анастасии):** утренний разбор в
08:00 МСК, проверка лидов каждый час с 08:00 до 23:00 МСК, проверка дедлайнов на
завтра в 17:00 МСК. Уведомления на телефон; на утренний разбор ещё и почта.
Сессии по расписанию идут **без MCP-инструментов**, поэтому все скрипты работают
через YouTrack REST и локальные файлы, а не через MCP — так и задумано, не ломать.

**Рамка от владельца (02.09.2026):** «Никто тебе ни сроков не ставил. У тебя есть
в договоре прописанный KPI к концу 3-го месяца. Ты сама себе должна план
составить достижения целей и по нему идти.» Отсюда правило приоритета: дело
попадает в «сегодня», только если приближает регистрацию до крайней даты окна
набора (см. PLAN.md).

## Календарь профильных конференций (проверенные реальные даты)

См. `docs/conference-calendar.md` — реальные даты подтверждены веб-поиском
(не выдумка), но перепроверять актуальность перед бронированием, так как
даты мероприятий могут сдвигаться.
