{
  "mcpServers": {
    "qubix": {
      "type": "http",
      "url": "https://dash.qubix.capital/api/mcp",
      "headers": {
        "Authorization": "Bearer ${QUBIX_API_TOKEN}"
      }
    },
    "masha": {
      "type": "http",
      "url": "https://masha.qubix.pro/",
      "headers": {
        "Authorization": "Bearer ${MASHA_API_TOKEN}"
      }
    },
    "youtrack": {
      "type": "http",
      "url": "https://team.qubix.capital/mcp",
      "headers": {
        "Authorization": "Bearer ${YOUTRACK_API_TOKEN}"
      }
    }
  }
}
